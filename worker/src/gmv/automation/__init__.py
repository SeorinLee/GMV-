"""Playwright automation for the TikTok Shop Affiliate Center (spec §11).

Pure helpers (``dom``) are importable without Playwright; the browser driver (``session``)
imports Playwright lazily so the rest of the package/tests do not require it.
"""
